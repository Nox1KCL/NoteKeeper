# NoteKeeper (Fault-Tolerant CLI Note Manager)

Консольний менеджер нотаток із фокусом на безпеку даних при аварійному завершенні роботи. Проєкт демонструє реалізацію механізмів **Fault Tolerance** у Python-додатках.

### 🛠 Технічні особливості (Reliability & Persistence):
* **Graceful Shutdown Handling:** Перехоплення системних переривань `KeyboardInterrupt` (Ctrl+C) для виконання аварійного збереження даних перед виходом (`quick_save`).
* **Session Persistence:** Автоматичне відновлення стану сесії (читання JSON) при запуску та гарантований запис при виході (`finally` block).
* **Modular Architecture:** Розділення логіки на шари: інтерфейс (`main.py`), бізнес-логіка (`noteMethods.py`) та I/O операції (`supportMethods.py`).

### 📋 Функціонал:
* **Emergency Save:** Якщо користувач пише нотатку і програма аварійно зупиняється, чернетка автоматично зберігається в базу.
* **CRUD Operations:** Повний цикл управління нотатками (створення, перегляд, редагування, видалення) з валідацією ID.
* **Auto-Recovery:** Автоматичне створення сховища даних (`userNotes.json`), якщо файл відсутній або пошкоджений.

### 🚀 Stack:
* **Language:** Python 3.13
* **Data:** JSON
* **Concepts:** Exception Handling, File I/O, Modular Design.
